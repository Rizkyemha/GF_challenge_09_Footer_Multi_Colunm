const Promotion_context = () => {
	return `
    <div class="promotion-context">
      <h3>Join our newslatter</h3>
      <p>we'll send you a nice letter once per week. No spam.</p>
    </div>
  `;
};

export default Promotion_context;
